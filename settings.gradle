dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url 'https://repo.spring.io/libs-release'}
        maven { url 'https://jitpack.io' }
        google()
        mavenCentral()
    }
}
include ':demo', ':evs_sdk'
